Draining HTTP response bodies matters in Cloudflare Workers because unread response streams can keep connections and underlying resources occupied longer than necessary. When a Worker decides not to use the body, explicitly consuming or cancelling it helps the runtime clean up promptly, which reduces the chance of stalled requests, avoids wasted resource usage, and keeps upstream fetch handling predictable under load.
